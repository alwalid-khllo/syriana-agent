name: web-xai
version: 1.0.0
description: "xAI Web Search — search the web via Grok's agentic web_search tool (Responses API). Requires xAI Grok OAuth (via `syriana auth`) or XAI_API_KEY (https://x.ai)."
author: SyrianaAIResearch
kind: backend
provides_web_providers:
  - xai
